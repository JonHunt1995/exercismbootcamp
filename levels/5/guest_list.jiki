// The on_guest_list function has 2 inputs
// The first input is the guest list.
// The second input is the name of the person.
// Returns whether or not a person is on the list as a boolean
function on_guest_list with guest_list, name do
  for each guest in guest_list do
    if guest == name do
      return true
    end
  end
  return false
end