// Receives a number as its input
// Should return the equivelent raindrop sounds
function raindrops with number do
  set output to ""
  if number % 3 == 0 do
    change output to join(output, "Pling")
  end
  if number % 5 == 0 do
    change output to join(output, "Plang")
  end
  if number % 7 == 0 do 
    change output to join(output, "Plong")
  end
  if output == "" do
    return number_to_string(number)
  end
  return output
end